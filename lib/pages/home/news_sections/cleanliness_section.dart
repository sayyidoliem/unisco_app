import 'package:flutter/material.dart';

class CleanlinessNewsSection extends StatelessWidget {
  const CleanlinessNewsSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Ini berita seksi kebersihan"),
    );
  }
}
