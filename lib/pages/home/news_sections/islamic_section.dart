import 'package:flutter/material.dart';

class IslamicNewsSection extends StatelessWidget {
  const IslamicNewsSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Ini berita seksi islam"),
    );
  }
}
