import 'package:flutter/material.dart';

class HumasNewsSection extends StatelessWidget {
  const HumasNewsSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Ini berita seksi humas"),
    );
  }
}
