import 'package:flutter/material.dart';

class CreativityNewsSection extends StatelessWidget {
  const CreativityNewsSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Ini berita seksi kreatifitas"),
    );
  }
}
