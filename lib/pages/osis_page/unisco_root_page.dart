import 'package:flutter/cupertino.dart';

class UniscoPage extends StatelessWidget {
  const UniscoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("UNISCO PAGE"),
    );
  }
}
